const Header = () => {
    return (
        <header>
            <div className="container">
                {/* Global header component */}
                <h1>GitHub API Example</h1>
            </div>
        </header>
    );
};

export default Header;
